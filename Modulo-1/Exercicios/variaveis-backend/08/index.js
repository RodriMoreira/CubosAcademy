let raioCirculo = 1;
let comprimentoCirculo = (2 * Math.PI * raioCirculo).toFixed(2);
let areaCirculo = (Math.PI * (Math.pow(raioCirculo, 2))).toFixed(2)

console.log(`Raio do círculo = ${raioCirculo} \nComprimento do círculo = ${comprimentoCirculo} \nÁrea de círculo = ${areaCirculo} `)